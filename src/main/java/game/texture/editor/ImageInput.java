package game.texture.editor;

import common.KeyInput;

public enum ImageInput implements KeyInput
{
	RESET_CAMERA,
	UNDO,
	REDO,
	FILL_SELECTION,
	FILL_SELECTED_AREA,
	FILL_DESELECTED_AREA,
	CLEAR_SELECTION,
	TOGGLE_GRID,
	TOGGLE_BACKGROUND
}
